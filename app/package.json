{
  "name": "edgar",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && npm run serve",
    "build": "tsc",
    "serve": "node build/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^8.0.13",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "express": "^4.15.3"
  }
}
